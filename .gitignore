node_modules/
dist/
.gradient/
*.log
.DS_Store
.worktrees/

# Claude Code state. The legacy command target and locally installed hooks are
# per-developer artifacts. Skills and rules are intentionally not ignored so a
# user can choose to review and share them; `gradient bundle` is the explicit
# team-distribution path.
.claude/worktrees/
.claude/settings.json
.claude/settings.local.json
.claude/commands/

# The marketing site lives in a separate private repo (gradient-web).
# Guard against it being re-added to this public repo.
webapp/

# Claude Code transcript exports use a timestamped slug filename. Keep raw
# sessions local: they can contain account details and unrelated conversation.
/20??-??-??-??????-*.txt
